<p align="center">
  <img src="docs/assets/raktajs_benner.png" alt="Rakta.js banner" width="100%" />
</p>

<h1 align="center">Rakta.js | React Framework</h1>

<p align="center">
  <strong>A lightweight, composable frontend framework built on Bun, React, and TypeScript.</strong><br />
  <strong>Framework frontend ringan yang dibangun di atas Bun, React, dan TypeScript.</strong>
</p>

<p align="center">
  <em>Small in size. Fierce in speed. Alive in every route.</em><br />
  <em>Kecil ukuran. Ganas kecepatan. Hidup di setiap route.</em>
</p>

<p align="center">
  <a href="https://github.com/RheinSullivan/raktajs/stargazers">
    <img src="https://img.shields.io/github/stars/RheinSullivan/raktajs?style=flat&logo=github&label=stars&color=eab308&labelColor=555" alt="GitHub stars" />
  </a>
  <a href="https://github.com/RheinSullivan/raktajs/graphs/contributors">
    <img src="https://img.shields.io/github/contributors/RheinSullivan/raktajs?style=flat&logo=github&label=contributors&color=9333ea&labelColor=555" alt="GitHub contributors" />
  </a>
  <a href="https://github.com/RheinSullivan/raktajs/commits/main">
    <img src="https://img.shields.io/github/commit-activity/m/RheinSullivan/raktajs?style=flat&logo=git&label=commit%20activity&color=F05032&labelColor=555" alt="Monthly commit activity" />
  </a>
  <a href="https://github.com/RheinSullivan/raktajs/commits/main">
    <img src="https://img.shields.io/github/last-commit/RheinSullivan/raktajs?style=flat&logo=github&label=last%20commit&color=28A745&labelColor=555" alt="Last commit" />
  </a>
  <a href="https://www.npmjs.com/package/raktajs">
    <img src="https://img.shields.io/npm/v/raktajs?style=flat&label=Rakta.js&color=C60005&labelColor=555" alt="raktajs npm version" />
  </a>
  <a href="https://www.npmjs.com/package/create-rakta-app">
    <img src="https://img.shields.io/npm/v/create-rakta-app?style=flat&label=create-rakta-app&color=C60005&labelColor=555" alt="create-rakta-app npm version" />
  </a>
  <a href="https://www.npmjs.com/package/raktajs">
    <img src="https://img.shields.io/npm/dm/raktajs?style=flat&label=rakta.js/month&color=009688&labelColor=555" alt="raktajs monthly downloads" />
  </a>
  <a href="https://www.npmjs.com/package/create-rakta-app">
    <img src="https://img.shields.io/npm/dm/create-rakta-app?style=flat&label=create-rakta-app/month&color=009688&labelColor=555" alt="create-rakta-app monthly downloads" />
  </a>
  <a href="https://bun.sh">
    <img src="https://img.shields.io/badge/Bun-1.3.11-db2777?style=flat&logo=bun&logoColor=white&labelColor=555" alt="Bun 1.3.11" />
  </a>
  <a href="https://react.dev">
    <img src="https://img.shields.io/badge/React-19.2.7-61DAFB?style=flat&logo=react&logoColor=white&labelColor=555" alt="React 19.2.7" />
  </a>
  <a href="https://www.typescriptlang.org">
    <img src="https://img.shields.io/badge/TypeScript-6.0.3-3178C6?style=flat&logo=typescript&logoColor=ffffff&labelColor=555" alt="TypeScript 6.0.3" />
  </a>
  <a href="./LICENSE">
    <img src="https://img.shields.io/github/license/RheinSullivan/raktajs?style=flat&label=license&color=7CB342&labelColor=555" alt="MIT License" />
  </a>
</p>

<p align="center">
  <strong>Bun in the engine. React at the core. Cirebon in the soul. Garuda in the heart.</strong><br />
  <strong>Bun di mesin. React di inti. Cirebon di jiwa. Garuda di hati.</strong>
</p>

---

Assalamu'alaikum Warahmatullahi Wabarakatuh · Shalom · Om Swastiastu · Namo Buddhaya · Wei De Dong Tian

🇮🇩 Dibuat dengan bangga dari Cirebon & Jakarta Selatan, Nusantara, Indonesia. 🇵🇸 Free Palestine.

---

## Story & Vision / Kisah & Visi

<table>
  <tr>
  <td width="50%" valign="top">

  **English**

  Rakta.js was created by **Muhammad Rizky Ramadhan**, a software developer from **Cirebon & South Jakarta, Nusantara, Indonesia**, known in the developer community as **Rhein Sullivan**, lead of **Vyagra Nexus™**.

  The vision behind Rakta.js is simple: stop repeating the same setup across every project. Features like *file-based routing*, *auto-import with zero manual import statements*, *type-safe RPC*, an integrated *frontend-backend monolith* architecture, and *built-in authentication* are all available in one unified package no fragmentation, no layered configuration, no fighting your tools.

  Rakta.js does not aim to replace frameworks like Next.js, Nuxt, or Remix. It is a different take: **a framework that feels clean, fast, and has its own identity**, built by an Indonesian developer who wanted something simpler and more honest.

  </td>
  <td width="50%" valign="top">

  **Bahasa Indonesia**

  Rakta.js diciptakan oleh **Muhammad Rizky Ramadhan**, pengembang perangkat lunak asal **Cirebon & Jakarta Selatan, Nusantara, Indonesia**, yang dikenal di komunitas developer sebagai **Rhein Sullivan**, ketua dari tim **Vyagra Nexus™**.

  Visi di balik Rakta.js sederhana: berhenti mengulang setup yang sama di setiap project. Fitur-fitur seperti *routing berbasis file*, *auto-import tanpa satu pun pernyataan import manual*, *RPC type-safe*, arsitektur *monolith frontend-backend*, dan *autentikasi bawaan* tersedia dalam satu paket terpadu tanpa fragmentasi, tanpa konfigurasi berlapis, tanpa bersaing dengan tools sendiri.

  Rakta.js tidak hadir untuk menggantikan Next.js, Nuxt, atau Remix. Ini adalah perspektif berbeda: **framework yang terasa bersih, cepat, dan punya identitas sendiri**, dibuat oleh developer Indonesia yang ingin sesuatu yang lebih sederhana dan lebih jujur.

  </td>
  </tr>
</table>

---

## About Rakta.js / Tentang Rakta.js

**English**

Rakta.js is a lightweight, composable frontend framework built on React, Bun, and TypeScript. It provides file-based routing, flexible rendering modes (CSR, SSR, SSG, SPA, Hybrid), SEO tooling, PWA support, type-safe RPC, state management, schema validation, an HTTP client, and a project generator all in one package.

The name **Rakta** comes from Sanskrit. It means red, life energy, courage, strength, identity, and movement. There is also a quiet reminder inside this name: when ordinary people carry heavy days in silence, every small tool should help them build something that truly belongs to them not something that keeps them waiting at the gate.

The shrimp mascot represents Cirebon, a coastal city in West Java, Indonesia. Cirebon is known for its shrimp culture, the *Trusmi* batik craftsmanship, *Mega Mendung* cloud motifs, and its royal kraton heritage. Rakta.js uses this identity as a living foundation not as decoration, but as a real part of who built it and why.

**Bahasa Indonesia**

Rakta.js adalah framework frontend ringan yang dibangun di atas React, Bun, dan TypeScript. Tersedia routing berbasis file, berbagai mode rendering (CSR, SSR, SSG, SPA, Hybrid), SEO tooling, PWA, RPC type-safe, state management, validasi schema, HTTP client, dan generator project semuanya dalam satu paket.

Nama **Rakta** berasal dari bahasa Sansekerta. Artinya merah, energi hidup, keberanian, kekuatan, identitas, dan gerak. Di dalam nama ini juga ada pengingat kecil: ketika orang biasa menanggung hari yang berat dalam diam, setiap tool kecil seharusnya membantu mereka membangun sesuatu yang benar-benar milik mereka bukan sesuatu yang membuat mereka terus menunggu di depan gerbang.

Maskot udang mewakili Cirebon, kota pesisir di Jawa Barat. Cirebon dikenal dengan budaya udangnya, kerajinan batik Trusmi, motif Mega Mendung, dan warisan keraton. Rakta.js menggunakan identitas ini sebagai fondasi yang hidup bukan sekadar hiasan, melainkan bagian nyata dari siapa yang membangunnya dan mengapa.

---

## Why Rakta.js? / Kenapa Rakta.js?

**English** Every problem in the table below is a real pain point that comes up when building modern web apps. This is how Rakta.js addresses each one.

**Bahasa Indonesia** Setiap masalah di tabel bawah adalah titik nyeri nyata yang muncul saat membangun aplikasi web modern. Begini cara Rakta.js mengatasinya.

| Problem / Masalah | Rakta.js Approach / Pendekatan |
| --- | --- |
| Frontend projects often start with too much setup / Project frontend sering butuh setup yang panjang | `create-rakta-app` generates a ready-to-run app in seconds / menghasilkan app siap jalan dalam hitungan detik |
| Routing setup becomes repetitive across every page / Setup routing berulang di setiap halaman | **MegaWeave** provides automatic file-based routing / menyediakan routing otomatis berbasis file |
| Navigation and image primitives feel too generic / Primitif navigasi dan gambar terasa generik | **ShrimpStep** (`<click>`) and **TrusmiFrame** (`<picture>`) have Rakta.js identity / punya identitas Rakta.js |
| SEO always needs extra configuration / SEO selalu butuh konfigurasi tambahan | **MegaSignal** handles metadata, sitemap, robots, RSS, and JSON-LD / mengelola metadata, sitemap, robots, RSS, dan JSON-LD |
| PWA is usually added as an afterthought / PWA biasanya ditambahkan belakangan | **ShrimpHarbor** is the built-in offline and installable app layer / layer offline dan installable app bawaan |
| Build tooling feels heavy / Build tooling terasa berat | **CherbonsEngine** runs on Bun-powered tooling / berjalan di atas tooling berbasis Bun |
| Runtime strategy needs to stay flexible / Strategi runtime perlu fleksibel | **NorthCoastFlow** manages request flow and rendering strategy / mengelola request flow dan strategi rendering |
| Type-safe RPC usually needs a separate library / RPC type-safe biasanya butuh library terpisah | **NagaLimanWire** is the built-in type-safe RPC system / adalah sistem RPC type-safe bawaan |
| Projects need build and route diagnostics / Project butuh diagnostik build dan route | **JatiLens** is the project health and diagnostics layer / adalah layer kesehatan dan diagnostik project |
| Framework integrations need predictable lifecycle / Integrasi framework butuh lifecycle yang bisa diprediksi | **RaktaKernel** coordinates services, environment, and plugins / mengkoordinasi services, environment, dan plugin |
| Apps need fine-grained request control / App butuh kontrol request yang presisi | **RaktaMiddleware** supports before, after, redirect, rewrite, and abort / mendukung before, after, redirect, rewrite, dan abort |
| Documentation needs to stay Markdown-first / Dokumentasi harus tetap berbasis Markdown | **RaktaDocs** generates sidebar, search index, and VitePress config / menghasilkan sidebar, search index, dan config VitePress |

---

## Feature Identity / Identitas Fitur

**English** Rakta.js uses its own naming system for all public features. The names are inspired by the cultural heritage of Cirebon and the wider Nusantara region, so they are memorable, unique, and carry real meaning not just generic tech names.

**Bahasa Indonesia** Rakta.js menggunakan sistem penamaan sendiri untuk semua fitur publik. Nama-nama ini terinspirasi dari warisan budaya Cirebon dan Nusantara, sehingga mudah diingat, unik, dan punya makna nyata bukan sekadar nama teknologi generik.

| Feature / Fitur | Purpose (EN) | Fungsi (ID) |
| --- | --- | --- |
| **MegaWeave** | File-based routing layer pages are discovered automatically from the `app/` folder | Layer routing berbasis file halaman ditemukan otomatis dari folder `app/` |
| **ShrimpStep** | Navigation component using `<click to="">` syntax, handles SPA routing without full page reload | Komponen navigasi dengan sintaks `<click to="">`, mengelola routing SPA tanpa reload halaman |
| **TrusmiFrame** | Optimized image component using `<picture path="">`, named after Trusmi batik | Komponen gambar teroptimasi dengan `<picture path="">`, dinamai dari batik Trusmi |
| **PanturaScroll** | Smooth scroll and in-page section navigation using `<pantura to="">` / `<reborns id="">`, named after Jalur Pantura, the legendary north coast highway of Java | Smooth scroll dan navigasi bagian halaman menggunakan `<pantura to="">` / `<reborns id="">`, dinamai dari Jalur Pantura, jalan pantai utara Jawa yang legendaris |
| **KasepuhanGate** | File-based API endpoint system, named after Kasepuhan Palace in Cirebon | Sistem endpoint API berbasis file, dinamai dari Keraton Kasepuhan Cirebon |
| **KanomanShield** | Route protection and authentication guard layer, named after Kanoman Palace | Layer proteksi route dan autentikasi, dinamai dari Keraton Kanoman |
| **SunyaragiCrown** | Metadata and `<head>` manager for SEO, named after Taman Sari Gua Sunyaragi | Manajer metadata dan `<head>` untuk SEO, dinamai dari Taman Sari Gua Sunyaragi |
| **NagaLimanWire** | Type-safe RPC connecting backend to frontend, named after the mythical Paksi Naga Liman creature | RPC type-safe yang menghubungkan backend ke frontend, dinamai dari makhluk mitologis Paksi Naga Liman |
| **TrusmiThread** | Auto-import scanner that generates typed declarations for all components and utilities | Scanner auto-import yang menghasilkan deklarasi bertype untuk semua komponen dan utility |
| **CherbonsEngine** | Build and dev engine powered by Bun, Vite, and esbuild | Engine build dan dev berbasis Bun, Vite, dan esbuild |
| **NorthCoastFlow** | Runtime rendering flow manager controls SSR, CSR, SSG, and hybrid strategies | Manajer alur rendering runtime mengontrol strategi SSR, CSR, SSG, dan hybrid |
| **MegaSignal** | Full SEO layer sitemap, robots.txt, RSS, JSON-LD, Open Graph, Twitter card | Layer SEO lengkap sitemap, robots.txt, RSS, JSON-LD, Open Graph, Twitter card |
| **ShrimpHarbor** | PWA and offline layer service worker, web manifest, cache strategies, install prompt | Layer PWA dan offline service worker, web manifest, strategi cache, install prompt |
| **JatiLens** | DX diagnostics bundle analyzer, route inspector, dependency graph, performance profiler | Diagnostik DX bundle analyzer, route inspector, dependency graph, performance profiler |
| **RaktaKernel** | Dependency injection container, service registry, environment manager, and plugin system | Container dependency injection, registry service, manajer environment, dan sistem plugin |
| **RaktaMiddleware** | Async request middleware pipeline with full lifecycle control | Pipeline middleware request async dengan kontrol lifecycle penuh |
| **RaktaDocs** | Markdown documentation manifest generator and VitePress bridge | Generator manifest dokumentasi Markdown dan jembatan VitePress |

---

## Installation / Instalasi

**English** There are multiple ways to create a new Rakta.js project. The recommended way is using Bun, but npm, pnpm, and yarn all work too.

**Bahasa Indonesia** Ada beberapa cara untuk membuat project Rakta.js baru. Cara yang direkomendasikan adalah menggunakan Bun, tapi npm, pnpm, dan yarn juga bisa.

### Using Bun (Recommended / Direkomendasikan)

```bash
bun create rakta-app@latest my-app
```

### Using npm

```bash
npm create rakta-app@latest my-app
```

### Using pnpm

```bash
pnpm create rakta-app@latest my-app
```

### Using Yarn

```bash
yarn create rakta-app my-app
```

### Using bunx / npx directly

```bash
# Bun
bunx create-rakta-app@latest my-app

# npm
npx create-rakta-app@latest my-app
```

After creating the project, move into the folder and start the dev server:

Setelah membuat project, masuk ke folder dan jalankan dev server:

```bash
cd my-app
bun install
bun run dev
```

The dev server runs at `http://localhost:3000` by default.

Dev server berjalan di `http://localhost:3000` secara default.

---

## Create App Modes / Mode Project

**English** When you run `create-rakta-app`, you will be asked to choose between two project modes. Both modes are production-ready out of the box.

**Bahasa Indonesia** Saat menjalankan `create-rakta-app`, kamu akan diminta memilih antara dua mode project. Keduanya langsung siap produksi tanpa konfigurasi tambahan.

### Frontend Only

**English** Use this when you are building a single-page application (SPA) or a frontend-only site. No backend is generated. The project root itself is the app.

**Bahasa Indonesia** Gunakan ini saat kamu membangun aplikasi satu halaman (SPA) atau situs frontend-only. Tidak ada backend yang dibuat. Root project itu sendiri adalah appnya.

```bash
bun create rakta-app@latest my-frontend
# Select: Frontend only
```

Generated structure / Struktur yang dihasilkan:

```txt
my-frontend/
├─ app/
│  ├─ page.tsx          Halaman utama / Main page
│  ├─ layout.tsx        Layout wrapper global
│  ├─ loading.tsx       Tampilan loading
│  ├─ error.tsx         Tampilan error
│  └─ not-found.tsx     Halaman 404
├─ app/components/      Komponen UI
├─ app/lib/             Helper, utilities, data
├─ app/hooks/           Custom React hooks
├─ public/              Static assets
├─ styles/
│  └─ globals.css       Global CSS
├─ rakta.config.ts      Konfigurasi framework
├─ package.json
└─ tsconfig.json
```

This mode does **not** generate: `backend/`, `database/`, `schema/`, `prisma/`, `shared/`, or any server-only files.

Mode ini **tidak** menghasilkan: `backend/`, `database/`, `schema/`, `prisma/`, `shared/`, atau file server-only apapun.

### Fullstack App

**English** Use this when you need both a frontend and a backend in one monorepo. The backend uses **Gaman.js** a Cirebon-named lightweight Express-like server framework included with Rakta.js. This mode gives you authentication, database adapter contracts, and shared types out of the box.

**Bahasa Indonesia** Gunakan ini saat kamu butuh frontend dan backend sekaligus dalam satu monorepo. Backend menggunakan **Gaman.js** framework server ringan bergaya Express yang dinamai dari Cirebon, sudah termasuk dalam Rakta.js. Mode ini memberikan autentikasi, kontrak adapter database, dan shared types langsung siap pakai.

```bash
bun create rakta-app@latest my-fullstack
# Select: Fullstack app
```

Generated structure / Struktur yang dihasilkan:

```txt
my-fullstack/
├─ frontend/
│  ├─ app/              Halaman dan routing
│  ├─ app/components/   Komponen UI
│  ├─ app/lib/          Helper dan data
│  ├─ app/hooks/        Custom hooks
│  ├─ public/
│  ├─ styles/
│  └─ rakta.config.ts
├─ backend/
│  ├─ src/
│  │  ├─ index.ts       Entry point server
│  │  ├─ routes/        API route handlers
│  │  └─ middleware/    Server middleware
│  └─ package.json
├─ shared/              Types dan contracts bersama
├─ docs/
├─ package.json
└─ tsconfig.base.json
```

---

## Rendering Modes / Mode Rendering

**English** Rakta.js supports multiple rendering strategies. You can set a default for the whole app and override it per route. This means one app can have a static homepage, a server-rendered dashboard, and a client-rendered user profile all at the same time.

**Bahasa Indonesia** Rakta.js mendukung berbagai strategi rendering. Kamu bisa mengatur default untuk seluruh app dan menggantinya per route. Artinya satu app bisa punya homepage statis, dashboard server-rendered, dan profil pengguna client-rendered semuanya sekaligus.

| Mode | Description (EN) | Deskripsi (ID) |
| --- | --- | --- |
| `csr` | Rendered entirely in the browser. Good for dynamic apps that do not need SEO. | Dirender sepenuhnya di browser. Cocok untuk app dinamis yang tidak butuh SEO. |
| `ssr` | Rendered on the server per request. Good for dynamic pages that need SEO. | Dirender di server per request. Cocok untuk halaman dinamis yang butuh SEO. |
| `ssg` | Rendered at build time. Good for pages that rarely change, like a blog or docs. | Dirender saat build. Cocok untuk halaman yang jarang berubah, seperti blog atau docs. |
| `csg` | Configurable static rendered at build time with optional client hydration. | Statis yang bisa dikonfigurasi dirender saat build dengan hidrasi client opsional. |
| `spa` | Full single-page application. All routing happens in the browser. | Single-page application penuh. Semua routing terjadi di browser. |
| `hybrid` | Mix rendering strategies per route in a single app. | Campur strategi rendering per route dalam satu app. |

Example configuration / Contoh konfigurasi:

```ts
// rakta.config.ts
import { defineConfig } from "rakta/config";

export default defineConfig({
  appName: "My Rakta App",
  render: {
    defaultMode: "ssr",  // Default untuk semua route / Default for all routes
    routes: {
      "/": "ssg",         // Homepage dirender saat build / Homepage rendered at build time
      "/dashboard": "csr", // Dashboard dirender di browser / Dashboard rendered in browser
      "/blog/**": "ssg",  // Semua halaman blog statis / All blog pages are static
      "/profile": "ssr",  // Profil dirender di server / Profile rendered on server
    },
  },
});
```

---

## Public Components / Komponen Publik

**English** Rakta.js provides its own custom HTML elements for navigation and images. These are not standard HTML tags they are Rakta.js framework elements that get compiled during the build. This means your code stays clean and framework-native.

**Bahasa Indonesia** Rakta.js menyediakan elemen HTML kustom sendiri untuk navigasi dan gambar. Ini bukan tag HTML standar melainkan elemen framework Rakta.js yang dikompilasi saat build. Artinya kode kamu tetap bersih dan native terhadap framework.

### `<click>` Navigation / Navigasi

**English** Use `<click to="">` instead of `<a href="">` for internal navigation. It handles SPA routing automatically, supports active state detection, prefetching, and keyboard accessibility out of the box.

**Bahasa Indonesia** Gunakan `<click to="">` sebagai ganti `<a href="">` untuk navigasi internal. Ini mengelola routing SPA secara otomatis, mendukung deteksi status aktif, prefetching, dan aksesibilitas keyboard tanpa konfigurasi tambahan.

```tsx
// Navigasi sederhana / Simple navigation
<click to="/about">About</click>

// Dengan active class / With active class
<click to="/dashboard" activeClassName="text-red-500">
  Dashboard
</click>

// Dengan prefetch / With prefetch
<click to="/products" prefetch>
  Products
</click>

// Link eksternal (otomatis buka tab baru) / External link (automatically opens new tab)
<click to="https://github.com/RheinSullivan/raktajs">
  GitHub
</click>
```

### `<picture>` Optimized Image / Gambar Teroptimasi

**English** Use `<picture path="">` for images. It handles responsive sizing, lazy loading, and format optimization.

**Bahasa Indonesia** Gunakan `<picture path="">` untuk gambar. Ini mengelola ukuran responsif, lazy loading, dan optimasi format.

```tsx
<picture path="/shrimp.png" alt="Rakta.js shrimp mascot" />

// Dengan ukuran kustom / With custom size
<picture path="/banner.jpg" alt="Banner" width={800} height={400} />
```

### `<pantura>` / `<reborns>` Smooth Scroll

**English** Use `<pantura to="">` for smooth in-page navigation to a specific section, and `<reborns id="">` to mark the target. Named after Jalur Pantura (Pantai Utara), the iconic north coast highway of Java that connects cities along the coastline.

**Bahasa Indonesia** Gunakan `<pantura to="">` untuk navigasi halus ke bagian tertentu di halaman yang sama, dan `<reborns id="">` untuk menandai target. Dinamai dari Jalur Pantura (Pantai Utara), jalan pantai utara Jawa yang ikonik dan menghubungkan kota-kota pesisir.

```tsx
// Scroll ke section / Scroll to section
<pantura to="contact">Contact Us</pantura>

// Dengan offset (berguna jika ada sticky navbar) / With offset (useful with sticky navbar)
<pantura to="features" offset={80} duration={600}>
  See Features
</pantura>

// Tandai target section / Mark target section
<reborns id="contact">
  <h2>Contact Us</h2>
</reborns>
```

### Programmatic Scroll Hook

```ts
import { usePantura } from "rakta/components";

function Navbar() {
  const scrollTo = usePantura({ offset: 80 });

  return (
    <button onClick={() => scrollTo("features")}>
      Jump to Features
    </button>
  );
}
```

---

## Schema Validation / Validasi Schema

**English** Rakta.js includes a built-in schema validation layer. You can validate user input, API responses, form data, or any data structure without adding a separate library like Zod or Yup. The API is designed to be readable and composable.

**Bahasa Indonesia** Rakta.js punya layer validasi schema bawaan. Kamu bisa memvalidasi input pengguna, respons API, data form, atau struktur data apapun tanpa menambahkan library terpisah seperti Zod atau Yup. API-nya dirancang agar mudah dibaca dan bisa dikombinasikan.

```ts
import { object, string, number, array, boolean } from "rakta/schema";

// Definisikan schema / Define schema
const userSchema = object({
  name: string().min(1).max(100),
  email: string().email(),
  age: number().int().min(0).max(150),
  isActive: boolean(),
  tags: array(string()),
});

// Validasi data / Validate data
const result = userSchema.safeParse({
  name: "Rhein Sullivan",
  email: "rhein@example.com",
  age: 22,
  isActive: true,
  tags: ["developer", "cirebon"],
});

if (result.kind === "success") {
  // result.data sudah bertype penuh / result.data is fully typed
  console.log(result.data.name); // "Rhein Sullivan"
} else {
  // result.errors berisi detail error per field / result.errors contains per-field error details
  console.error(result.errors);
}
```

---

## Type-Safe RPC / RPC Type-Safe

**English** **NagaLimanWire** is Rakta.js's built-in RPC system. It lets you define server-side procedures and call them from the frontend as if they were local functions with full TypeScript type safety across the network boundary. No manual API route writing, no type duplication, no `fetch` boilerplate.

**Bahasa Indonesia** **NagaLimanWire** adalah sistem RPC bawaan Rakta.js. Ini memungkinkan kamu mendefinisikan prosedur sisi server dan memanggilnya dari frontend seolah-olah seperti fungsi lokal dengan type safety TypeScript penuh melewati batas jaringan. Tidak perlu menulis route API manual, tidak ada duplikasi type, tidak ada boilerplate `fetch`.

**Server Define procedures / Definisikan prosedur:**

```ts
// backend/src/router.ts
import { createRaktaRouter, publicProcedure, protectedProcedure } from "rakta/rpc";
import { object, string, number } from "rakta/schema";

export const appRouter = createRaktaRouter({
  // Prosedur publik siapa saja bisa akses / Public procedure anyone can access
  getUser: publicProcedure
    .input(object({ id: string() }))
    .query(async ({ input }) => {
      // Ambil user dari database / Fetch user from database
      return { id: input.id, name: "Rhein", role: "admin" };
    }),

  // Prosedur dengan mutation / Mutation procedure
  createPost: protectedProcedure
    .input(object({
      title: string().min(1),
      content: string().min(10),
    }))
    .mutation(async ({ input, ctx }) => {
      // ctx.user tersedia karena protectedProcedure / ctx.user available because of protectedProcedure
      return { id: "new-post-id", ...input, authorId: ctx.user.id };
    }),
});

// Export type untuk digunakan di frontend / Export type for use on frontend
export type AppRouter = typeof appRouter;
```

**Client Call procedures / Panggil prosedur:**

```ts
// frontend/app/page.tsx
import { createRaktaClient } from "rakta/rpc";
import type { AppRouter } from "../../backend/src/router";

const api = createRaktaClient<AppRouter>({
  baseUrl: "http://localhost:3000/rpc",
});

// TypeScript tahu persis shape dari return value / TypeScript knows exact shape of return value
const user = await api.getUser.query({ id: "user-123" });
console.log(user.name); // TypeScript autocomplete bekerja / TypeScript autocomplete works

// Mutation dengan type safety penuh / Mutation with full type safety
const post = await api.createPost.mutate({
  title: "Hello Rakta.js",
  content: "This is my first post using Rakta.js RPC.",
});
```

---

## Kernel & Plugin System / Sistem Kernel & Plugin

**English** The **RaktaKernel** is the dependency injection and service container at the core of Rakta.js. It manages your app's services (database connections, auth adapters, config values), controls the environment, and provides a predictable lifecycle for plugins. If you have used Spring Boot's application context or Nest.js's module system, this will feel familiar but much simpler.

**Bahasa Indonesia** **RaktaKernel** adalah dependency injection dan service container di inti Rakta.js. Ini mengelola services aplikasi kamu (koneksi database, auth adapter, nilai konfigurasi), mengontrol environment, dan menyediakan lifecycle yang bisa diprediksi untuk plugin. Jika kamu pernah pakai application context Spring Boot atau sistem modul Nest.js, ini akan terasa familiar tapi jauh lebih sederhana.

```ts
import { createRaktaKernel } from "rakta/kernel";

// Buat kernel dengan environment dan konfigurasi / Create kernel with environment and config
const kernel = createRaktaKernel({
  environmentName: "production",
  env: {
    DATABASE_URL: process.env.DATABASE_URL,
    API_KEY: process.env.API_KEY,
    APP_SECRET: process.env.APP_SECRET,
  },
});

// Daftarkan service sebagai singleton / Register service as singleton
// Singleton hanya dibuat sekali dan dipakai ulang / Singleton created once and reused
kernel.services.singleton("db", () => connectDatabase(
  kernel.environment.require("DATABASE_URL")
));

kernel.services.singleton("auth", () => createAuthAdapter({
  secret: kernel.environment.require("APP_SECRET"),
}));

// Daftarkan plugin / Register plugin
kernel.use({
  name: "analytics",
  // configure dipanggil saat kernel start / configure called when kernel starts
  configure(context) {
    context.registerFeature({ name: "analytics", enabled: true });
  },
  // ready dipanggil setelah semua plugin selesai configure / ready called after all plugins configured
  async ready() {
    console.log("Analytics plugin is ready.");
  },
});

// Start kernel menjalankan semua lifecycle hooks / Start kernel runs all lifecycle hooks
await kernel.start();

// Gunakan service yang sudah terdaftar / Use registered services
const db = kernel.services.resolve("db");
const auth = kernel.services.resolve("auth");
```

**Plugin Lifecycle / Lifecycle Plugin:**

| Phase | When it runs / Kapan berjalan | Purpose / Tujuan |
| --- | --- | --- |
| `configure` | At kernel start / Saat kernel start | Register features, services, middleware |
| `start` | After all plugins configured / Setelah semua plugin selesai configure | Connect to databases, start background tasks |
| `ready` | When app is fully initialized / Saat app sudah sepenuhnya siap | Log readiness, run health checks |
| `shutdown` | When app is shutting down / Saat app akan ditutup | Clean up connections, flush queues |

See full docs: [`docs/en/kernel.md`](./docs/en/kernel.md) / [`docs/id/kernel.md`](./docs/id/kernel.md)

---

## Middleware

**English** **RaktaMiddleware** gives you full control over every incoming request. You can run code before a route handler, after it, redirect users, rewrite URLs, abort requests, or chain multiple middleware together. All middleware is async first and type-safe.

**Bahasa Indonesia** **RaktaMiddleware** memberi kamu kontrol penuh atas setiap request yang masuk. Kamu bisa menjalankan kode sebelum route handler, sesudahnya, mengalihkan pengguna, menulis ulang URL, membatalkan request, atau menggabungkan beberapa middleware sekaligus. Semua middleware async first dan type-safe.

```ts
import { defineMiddleware, compose } from "rakta/middleware";

// Middleware autentikasi / Authentication middleware
const authMiddleware = defineMiddleware({
  name: "auth",
  async before({ request, next, redirect, abort }) {
    const token = request.headers.get("Authorization");

    if (!token) {
      // Arahkan ke halaman login / Redirect to login page
      return redirect("/login");
    }

    const user = await verifyToken(token);
    if (!user) {
      // Batalkan request dengan status 401 / Abort request with 401 status
      return abort(401, "Unauthorized");
    }

    // Lanjut ke handler berikutnya / Continue to next handler
    return next({ user });
  },
});

// Middleware logging / Logging middleware
const logMiddleware = defineMiddleware({
  name: "logger",
  async before({ request, next }) {
    console.log(`${request.method} ${request.url}`);
    const response = await next();
    console.log(`${response.status}`);
    return response;
  },
});

// Gabungkan middleware / Compose middleware
export const apiMiddleware = compose(logMiddleware, authMiddleware);
```

---

## SEO

**English** **MegaSignal** and **SunyaragiCrown** handle all SEO needs. You can define metadata at the page level, generate sitemaps automatically, control robots.txt, add JSON-LD structured data, and configure Open Graph and Twitter card tags all from TypeScript.

**Bahasa Indonesia** **MegaSignal** dan **SunyaragiCrown** mengelola semua kebutuhan SEO. Kamu bisa mendefinisikan metadata di level halaman, menghasilkan sitemap secara otomatis, mengontrol robots.txt, menambahkan data terstruktur JSON-LD, dan mengonfigurasi tag Open Graph dan Twitter card semuanya dari TypeScript.

```ts
// app/page.tsx metadata untuk halaman ini / page-level metadata
export const metadata = {
  title: "Rakta.js React Framework",
  description: "A lightweight, composable frontend framework built on Bun, React, and TypeScript.",
  canonical: "https://raktajs.dev",
  openGraph: {
    title: "Rakta.js React Framework",
    description: "Small in size. Fierce in speed. Alive in every route.",
    type: "website",
    image: "https://raktajs.dev/og-image.png",
  },
  twitter: {
    card: "summary_large_image",
    title: "Rakta.js",
    description: "Small in size. Fierce in speed.",
  },
  jsonLd: {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    name: "Rakta.js",
    applicationCategory: "DeveloperApplication",
  },
};

export default function HomePage() {
  return <main>...</main>;
}
```

---

## PWA

**English** **ShrimpHarbor** is the PWA and offline layer. It handles service worker registration, web manifest generation, cache strategies, and the install prompt without you having to write a single line of service worker code manually.

**Bahasa Indonesia** **ShrimpHarbor** adalah layer PWA dan offline. Ini mengelola registrasi service worker, pembuatan web manifest, strategi cache, dan install prompt tanpa kamu harus menulis satu baris pun kode service worker secara manual.

```ts
// rakta.config.ts
import { defineConfig } from "rakta/config";

export default defineConfig({
  pwa: {
    enabled: true,
    manifest: {
      name: "My Rakta App",
      short_name: "RaktaApp",
      theme_color: "#e11d48",
      background_color: "#000000",
      display: "standalone",
      icons: [
        { src: "/favicon.ico", sizes: "48x48", type: "image/x-icon" },
        { src: "/favicon-32x32.png", sizes: "32x32", type: "image/png" },
        { src: "/android-chrome-192x192.png", sizes: "192x192", type: "image/png" },
        { src: "/android-chrome-512x512.png", sizes: "512x512", type: "image/png", purpose: "any maskable" },
        { src: "/apple-touch-icon.png", sizes: "180x180", type: "image/png" },
        { src: "/rakta-logo.svg", sizes: "any", type: "image/svg+xml" },
      ],
    },
    // Strategi cache per tipe aset / Cache strategy per asset type
    cacheStrategy: {
      images: "cache-first",      // Gambar prioritaskan cache / Images cache first
      fonts: "cache-first",       // Font selalu dari cache / Fonts always from cache
      api: "network-first",       // API prioritaskan jaringan / API network first
      pages: "stale-while-revalidate", // Halaman tampil cache, update background
    },
    // Fallback saat offline / Offline fallback
    offlineFallback: "/offline",
  },
});
```

---

## HTTP Client / Klien HTTP

**English** **PanturaFetch** is the built-in HTTP client, named after Pantura (Pantai Utara / North Coast) the coastal highway of Java. It is a thin, typed wrapper around `fetch` with interceptor support, automatic JSON parsing, and error handling.

**Bahasa Indonesia** **PanturaFetch** adalah HTTP client bawaan, dinamai dari Pantura (Pantai Utara) jalan raya pesisir Jawa. Ini adalah wrapper tipis dan bertype di atas `fetch` dengan dukungan interceptor, parsing JSON otomatis, dan penanganan error.

```ts
import { createHttpClient } from "rakta/http";

const http = createHttpClient({
  baseUrl: "https://api.example.com",
  headers: { "Content-Type": "application/json" },
});

// Tambahkan interceptor / Add interceptors
http.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

http.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.status === 401) window.location.href = "/login";
    return Promise.reject(error);
  }
);

// Gunakan client / Use the client
const users = await http.get<User[]>("/users");
const newUser = await http.post<User>("/users", { name: "Rhein", role: "admin" });
```

---

## Auto Import / Auto-Import (TrusmiThread)

**English** **TrusmiThread** scans your project directories and automatically registers all components, hooks, stores, and utilities into the global TypeScript scope. This means you never need to write `import` statements for your own components just use them directly. Named after Trusmi, the historic batik weaving village of Cirebon.

**Bahasa Indonesia** **TrusmiThread** memindai direktori project kamu dan secara otomatis mendaftarkan semua komponen, hooks, store, dan utility ke dalam scope TypeScript global. Artinya kamu tidak perlu menulis pernyataan `import` untuk komponen sendiri langsung gunakan saja. Dinamai dari Trusmi, desa tenun batik bersejarah di Cirebon.

```ts
// rakta.config.ts
import { defineConfig } from "rakta/config";

export default defineConfig({
  autoImport: {
    enabled: true,
    // Direktori yang dipindai / Directories to scan
    directories: ["app", "components", "lib", "stores", "schemas", "utils"],
    // Lokasi file type yang dihasilkan / Location of generated type file
    outputDirectory: ".rakta",
    // Hasilkan file .d.ts untuk IDE autocomplete / Generate .d.ts for IDE autocomplete
    dts: true,
  },
});
```

After enabling auto-import, you can use components directly without imports:

Setelah mengaktifkan auto-import, kamu bisa langsung pakai komponen tanpa import:

```tsx
// components/Navbar.tsx
export default function Navbar() {
  return <nav>...</nav>;
}

// app/page.tsx Tidak ada import statement / No import statement needed
export default function HomePage() {
  return (
    <main>
      <Navbar />         {/* Langsung pakai, tanpa import / Use directly, no import */}
      <FeatureGrid />    {/* Sama / Same */}
      <Footer />         {/* Sama / Same */}
    </main>
  );
}
```

---

## State Management / Manajemen State

**English** Rakta.js includes a lightweight store built into `rakta/store`. It is a simple, typed reactive state container that works without any boilerplate. Think of it as a minimal Zustand-like store that is already part of the framework.

**Bahasa Indonesia** Rakta.js menyertakan store ringan yang sudah ada di `rakta/store`. Ini adalah state container reaktif sederhana dan bertype yang bekerja tanpa boilerplate apapun. Bayangkan seperti store minimal bergaya Zustand yang sudah menjadi bagian dari framework.

```ts
import { createRaktaStore } from "rakta/store";

// Definisikan store / Define store
const useCounterStore = createRaktaStore({
  // State awal / Initial state
  count: 0,
  label: "counter",
}, {
  // Actions
  increment: (state) => ({ count: state.count + 1 }),
  decrement: (state) => ({ count: state.count - 1 }),
  reset: () => ({ count: 0 }),
  setLabel: (state, label: string) => ({ label }),
});

// Gunakan di komponen / Use in component
function Counter() {
  const { count, label, increment, decrement, reset } = useCounterStore();

  return (
    <div>
      <p>{label}: {count}</p>
      <button onClick={increment}>+</button>
      <button onClick={decrement}>-</button>
      <button onClick={reset}>Reset</button>
    </div>
  );
}
```

---

## Repository Layout / Struktur Repository

**English** This is the recommended layout for the Rakta.js monorepo. Having a clear structure makes it easier to contribute, navigate, and maintain.

**Bahasa Indonesia** Ini adalah layout yang direkomendasikan untuk monorepo Rakta.js. Struktur yang jelas memudahkan kontribusi, navigasi, dan pemeliharaan.

```txt
raktajs/
├─ .github/
│  ├─ workflows/          CI/CD pipelines
│  └─ ISSUE_TEMPLATE/     Template issue GitHub
├─ docs/
│  ├─ en/                 29 dokumentasi Bahasa Inggris
│  └─ id/                 29 dokumentasi Bahasa Indonesia
├─ packages/
│  ├─ rakta/              Core framework package
│  └─ create-rakta/       CLI scaffolding tool
├─ templates/
│  ├─ frontendOnly/       Template starter SPA
│  └─ fullStack/          Template starter monolith
├─ scripts/               Build dan utility scripts
├─ .gitignore
├─ biome.json             Konfigurasi linter/formatter
├─ bunfig.toml            Konfigurasi Bun
├─ package.json           Workspace root
├─ README.md
├─ CONTRIBUTING.md
├─ SECURITY.md
├─ LICENSE
└─ tsconfig.base.json     TypeScript base config
```

---

## Scripts

**English** These are the available commands from the repository root. All scripts use Bun as the runtime.

**Bahasa Indonesia** Ini adalah perintah yang tersedia dari root repository. Semua script menggunakan Bun sebagai runtime.

```bash
# Jalankan semua package dalam mode development / Run all packages in development mode
bun run dev

# Build semua package untuk produksi / Build all packages for production
bun run build

# Cek TypeScript di semua package / Check TypeScript across all packages
bun run typecheck

# Jalankan semua test / Run all tests
bun run test

# Jalankan linter di seluruh codebase / Run linter across the entire codebase
bun run lint

# Format semua file / Format all files
bun run format

# Build hanya core framework / Build only the core framework
bun run build:rakta

# Build hanya CLI scaffolding tool / Build only the CLI scaffolding tool
bun run build:create-rakta

# TypeCheck hanya core framework / TypeCheck only the core framework
bun run typecheck:rakta

# TypeCheck hanya CLI / TypeCheck only the CLI
bun run typecheck:create-rakta
```

---

## Package Exports / Ekspor Package

**English** Rakta.js is designed with tree-shaking in mind. Every feature is available as a separate subpath export, so you only import what you actually use. Your final bundle will only include the parts of Rakta.js that your app needs.

**Bahasa Indonesia** Rakta.js dirancang dengan tree-shaking dalam pikiran. Setiap fitur tersedia sebagai subpath export terpisah, sehingga kamu hanya mengimpor apa yang benar-benar digunakan. Bundle akhir hanya akan menyertakan bagian Rakta.js yang dibutuhkan appmu.

```ts
import { defineConfig } from "rakta/config";          // Konfigurasi framework
import { createRaktaRouter } from "rakta/rpc";         // RPC system
import { object, string } from "rakta/schema";         // Schema validation
import { createRaktaKernel } from "rakta/kernel";      // DI container
import { defineMiddleware } from "rakta/middleware";    // Middleware
import { createHttpClient } from "rakta/http";         // HTTP client
import { createRaktaStore } from "rakta/store";        // State management
import { Pantura, Reborns, usePantura } from "rakta/components";  // Smooth scroll
import { Click, Picture } from "rakta/components";     // Navigation & image
import { RaktaAlert, RaktaToast } from "rakta/components"; // Alert & toast
import { useLengko, useEmpal } from "rakta/hooks";     // Framework hooks
import { defineSeo } from "rakta/seo";                 // SEO metadata
import { registerPwaWorker } from "rakta/pwa";         // PWA support
```

Full export list / Daftar ekspor lengkap:

```txt
rakta               Re-exports semua yang umum digunakan
rakta/config        defineConfig, defineRaktaConfig
rakta/router        generateManifest, scanRoutes, matchRoute
rakta/render        RenderMode types dan helpers
rakta/rpc           createRaktaRouter, createRaktaClient, publicProcedure
rakta/schema        object, string, number, boolean, array, union, literal
rakta/http          createHttpClient, PanturaFetch
rakta/store         createRaktaStore
rakta/components    Click, Picture, Pantura, Reborns, Alert, Toast
rakta/hooks         useLengko, useEmpal, useRakta, dan 30+ hooks lainnya
rakta/seo           defineSeo, RaktaHead, generateSitemap, generateRobots
rakta/pwa           registerPwaWorker, generateManifest, CacheStrategy
rakta/kernel        createRaktaKernel, RaktaPlugin type
rakta/middleware    defineMiddleware, compose, RaktaMiddlewareContext
rakta/auto-import   scanForExports, generateDts
rakta/layout        defineLayout, matchLayout, LayoutManifest
rakta/data          cache, revalidate, prefetch, CacheStrategy
rakta/docs          generateManifest, generateSidebar, generateVitePressConfig
rakta/security      createCsrf, createSecureHeaders, RateLimiter, SecretManager
rakta/ops           createEventBus, createJobQueue, createCron
rakta/performance   Benchmark, BundleAnalyzer, BuildCache
rakta/testing       createMockServer, createSnapshot, coverage
rakta/deployment    createDeploymentAdapter, listDeploymentTargets
rakta/plugin        createOfficialPlugin, createCommunityPlugin
rakta/dx            DependencyGraph, RouteAnalyzer, BundleReport
rakta/tide          Runtime request/response layer
rakta/forge         Dev server dan build system internals
```

---

## Roadmap

**English** The roadmap below is a public contract. What is listed here is what we intend to build. Versions are grouped by focus area, and each entry shows the key deliverables.

**Bahasa Indonesia** Roadmap di bawah ini adalah kontrak publik. Apa yang tercantum di sini adalah apa yang kami rencanakan untuk dibangun. Versi dikelompokkan berdasarkan area fokus, dan setiap entri menunjukkan deliverables utamanya.

### Released v1.0.0 through v1.0.4 / Sudah Rilis

| Version | Date | Changes |
| --- | --- | --- |
| **v1.0.0** | 2026-07 | First public release full module set: router, render, rpc, store, schema, http, seo, pwa, kernel, middleware, layout, data, dx, performance, security, ops, testing, deployment, plugin, docs |
| **v1.0.1** | 2026-07 | Biome lint fix indentation in `plugin/official.ts` |
| **v1.0.4** | 2026-07 | Add GSAP + ScrollTrigger, PanturaScroll smooth scroll (`<pantura>` / `<reborns>`), separate lib/helper files in templates, remove CSS from .tsx files, update documentation language |

### All Systems Shipped / Semua Sistem yang Sudah Ada

| System / Sistem | Module |
| --- | --- |
| Core configuration / Konfigurasi inti | `raktajs/config` |
| CLI starter + generator | `create-rakta-app` |
| File-based routing scanner | `raktajs/router` |
| Rendering modes (CSR, SPA, SSR, SSG, CSG, Hybrid) | `raktajs/render` |
| Schema validation / Validasi schema | `raktajs/schema` |
| Type-safe RPC | `raktajs/rpc` |
| Tide runtime layer | `raktajs/tide` |
| Forge dev server + build | `raktajs/forge` |
| SEO, sitemap, robots, JSON-LD | `raktajs/seo` |
| PWA and service worker | `raktajs/pwa` |
| Auto-import TrusmiThread | `raktajs/auto-import` |
| HTTP client PanturaFetch | `raktajs/http` |
| State management store | `raktajs/store` |
| Kernel, services, plugins | `raktajs/kernel` |
| Middleware pipeline | `raktajs/middleware` |
| Layout manifest + matcher | `raktajs/layout` |
| Data strategy contracts | `raktajs/data` |
| DX tools bundle/route analyzer | `raktajs/dx` |
| Performance benchmarks | `raktajs/performance` |
| Security CSP, CSRF, rate limiter | `raktajs/security` |
| Operations cron, queue, event bus | `raktajs/ops` |
| Testing package | `raktajs/testing` |
| Deployment adapters | `raktajs/deployment` |
| Plugin registry | `raktajs/plugin` |
| Docs manifest VitePress bridge | `raktajs/docs` |
| Smooth scroll PanturaScroll (`<pantura>` / `<reborns>`) | `raktajs/components` |
| Brutalist UI Alert & Toaster | `raktajs/components` |
| Frontend-only starter template | `create-rakta-app` |
| Fullstack starter template (Gaman.js + auth + CMS) | `create-rakta-app` |
| English + Indonesian documentation (29 files each) | `docs/en/` and `docs/id/` |

### Planned / Direncanakan

| Version | Focus / Fokus | Key Deliverables / Deliverables Utama |
| --- | --- | --- |
| **v1.0.x** | Patch & polish | Bug fixes, dependency updates, new Cirebon-named components, DX improvements no breaking changes |
| **v1.1** | Database adapters | SQLite, PostgreSQL, MySQL, and edge KV adapters with typed repository abstraction, ORM adapter contracts |
| **v1.2** | Auth providers | OAuth, email verification, refresh tokens, session rotation, magic link support |
| **v1.3** | Devtools UI | Browser-based auto-import inspector, route analyzer, bundle analyzer, performance profiler, error overlay |
| **v1.4** | Build compiler | Incremental compilation, persistent cache, chunk optimizer, prefetch manifest, parallel compilation |
| **v1.5** | Content layer | Markdown collections, MDX plugin bridge, typed content schema, live docs playground |
| **v2.0** | Ecosystem split | Official packages: `@rakta/auth`, `@rakta/database`, `@rakta/storage`, `@rakta/image`, `@rakta/forms`, `@rakta/analytics`, `@rakta/testing` |
| **v2.1** | Edge first | Cloudflare Workers / Deno Deploy streaming SSR, edge middleware, edge KV cache |
| **v2.2** | React Server Components | Server component rendering, server actions, Suspense streaming |
| **v2.3** | i18n | Built-in internationalization routing, locale detection, message catalog |
| **v2.4** | Native App Engine | Native compiler for iOS, Android, macOS, Windows, Linux. Sub-5MB shell, zero-latency IPC for device APIs (Camera, Biometrics, Push, Storage, Geolocation), 120 FPS hardware-accelerated web views lighter than Capacitor/Cordova |
| **v3.0** | Multi-runtime | React, Preact, Solid, and Vue as interchangeable rendering runtimes via plugin API |

---

## Contributing / Kontribusi

**English** Rakta.js is open for contributions from anyone. Whether you are fixing a bug, improving documentation, adding a new feature, or just reporting an issue all of it helps. Here is how to get started.

**Bahasa Indonesia** Rakta.js terbuka untuk kontribusi dari siapa saja. Baik kamu memperbaiki bug, meningkatkan dokumentasi, menambahkan fitur baru, atau sekadar melaporkan issue semuanya membantu. Begini cara memulainya.

### Setup Lokal / Local Setup

```bash
# Clone repository
git clone https://github.com/RheinSullivan/raktajs.git
cd raktajs

# Install semua dependencies / Install all dependencies
bun install

# Pastikan semua TypeScript type benar / Make sure all TypeScript types are correct
bun run typecheck

# Build semua package / Build all packages
bun run build

# Jalankan semua test untuk memastikan semuanya OK / Run all tests to confirm everything works
bun run test
```

### Buat Branch / Create a Branch

```bash
# Format nama branch: type/deskripsi-singkat
# Branch name format: type/short-description
git checkout -b feat/nama-fitur-baru
git checkout -b fix/perbaiki-bug-routing
git checkout -b docs/update-readme
```

### Format Commit / Commit Format

**English** We use conventional commits. This makes the git history easy to read and helps with automated changelog generation.

**Bahasa Indonesia** Kami menggunakan conventional commits. Ini membuat riwayat git mudah dibaca dan membantu pembuatan changelog otomatis.

```bash
# Fitur baru / New feature
feat(rakta): add smooth scroll offset support

# Perbaikan bug / Bug fix
fix(router): correct wildcard route matching order

# Perubahan dokumentasi / Documentation change
docs(readme): add bilingual contributing guide

# Perubahan build/config / Build or config change
chore(deps): update bun to 1.3.11

# Refactor tanpa perubahan fungsional / Refactor without functional change
refactor(kernel): split service container into separate file

# Perbaikan style/format / Style or format fix
style(components): fix biome import ordering
```

### Pull Request

**English** Before opening a PR, make sure `bun run typecheck`, `bun run lint`, and `bun run test` all pass locally. The CI will reject PRs that fail any of these checks.

**Bahasa Indonesia** Sebelum membuka PR, pastikan `bun run typecheck`, `bun run lint`, dan `bun run test` semuanya lulus di lokal. CI akan menolak PR yang gagal salah satu dari pengecekan ini.

---

## Donations & Humanitarian Support / Donasi & Dukungan Kemanusiaan

**English**

Rakta.js accepts voluntary donations. There is no obligation this framework is and will always be free and open source. But if you want to support the project, here is exactly how the money is used.

Every donation is split into two parts:

**Framework Operations Maximum 30%**

This covers the real costs of keeping the framework alive:
- Server hosting and bandwidth
- Domain registration and SSL certificate renewal
- CDN and package registry fees
- Security monitoring tools
- Development infrastructure

**Humanitarian Aid Minimum 70%**

The majority always goes to people who need it:
- Palestine 🇵🇸 support for our brothers and sisters under occupation
- Low-income families and the underprivileged (*kaum dhuafa*)
- Orphanages and children in care
- Elderly care homes (*panti jompo*)
- Disaster relief victims in Indonesia

**100% Transparency Commitment**

Every rupiah that comes in is recorded and reported publicly:
- Monthly financial reports are published in this repository
- Every expense has a receipt or proof of transfer
- Public audit is open to anyone at any time
- No deductions for personal benefit
- The 30% operational cap is a hard limit, not an estimate

The principle is simple: **everything that comes in is recorded, everything that goes out can be proven.**

---

**Bahasa Indonesia**

Rakta.js menerima donasi sukarela. Tidak ada kewajiban framework ini gratis dan open source selamanya. Tapi jika kamu ingin mendukung project ini, begini tepatnya cara uang itu digunakan.

Setiap donasi dibagi menjadi dua bagian:

**Operasional Framework Maksimal 30%**

Ini menutupi biaya nyata untuk menjaga framework tetap hidup:
- Server hosting dan bandwidth
- Pendaftaran domain dan perpanjangan SSL certificate
- Biaya CDN dan package registry
- Tools security monitoring
- Infrastruktur development

**Bantuan Kemanusiaan Minimal 70%**

Mayoritas selalu pergi ke orang yang membutuhkan:
- Palestina 🇵🇸 dukungan untuk saudara-saudara kita di bawah pendudukan
- Keluarga kurang mampu dan kaum dhuafa
- Panti asuhan dan anak-anak dalam perawatan
- Panti jompo dan lansia
- Korban bencana alam di Indonesia

**Komitmen Transparansi 100%**

Setiap rupiah yang masuk dicatat dan dilaporkan secara publik:
- Laporan keuangan bulanan dipublikasi di repository ini
- Setiap pengeluaran disertai bukti atau receipt transfer
- Audit publik terbuka untuk siapa saja kapan saja
- Tidak ada pemotongan untuk kepentingan pribadi
- Batas 30% operasional adalah batasan keras, bukan estimasi

Prinsipnya sederhana: **semua yang masuk tercatat, semua yang keluar bisa dibuktikan.**

---

### How to Donate / Cara Donasi

<p align="center">
  <a href="https://buymeacoffee.com/rheinsullivan" target="_blank">
    <img src="https://img.shields.io/badge/Support-Buy%20Me%20A%20Coffee-FFDD00?style=for-the-badge&logo=buy-me-a-coffee&logoColor=black" alt="Buy Me A Coffee" />
  </a>
</p>

- **Official donation link / Link donasi resmi:** [buymeacoffee.com/rheinsullivan](https://buymeacoffee.com/rheinsullivan)
- **Financial reports / Laporan keuangan:** folder `/docs/financial-reports/` or GitHub Issues with label `financial-report`
- **Full documentation / Dokumentasi lengkap:** [`docs/en/donations.md`](./docs/en/donations.md) / [`docs/id/donasi.md`](./docs/id/donasi.md)

### Distribution Partners / Mitra Distribusi

**English** Official organizations (orphanages, elderly care homes, NGOs, zakat foundations) that want to become a distribution partner for the humanitarian funds can apply through:

1. Open a GitHub Issue titled: `[Distribution Partner] Organization Name`
2. Attach: legal documents, address, assistance programs, and a distribution proposal
3. The team will review and coordinate the transfer with full public transparency

**Bahasa Indonesia** Lembaga resmi (panti asuhan, panti jompo, NGO, lembaga zakat) yang ingin menjadi mitra distribusi dana kemanusiaan dapat mendaftar melalui:

1. Buka GitHub Issue dengan judul: `[Mitra Distribusi] Nama Organisasi`
2. Lampirkan: dokumen legalitas, alamat, program bantuan, dan proposal distribusi
3. Tim akan mereview dan mengkoordinasi transfer dengan transparansi publik penuh

---

🇵🇸 **We believe that good technology is built with conscience. Free Palestine.**

🇵🇸 **Kami percaya bahwa teknologi yang baik dibangun bersama hati nurani. Free Palestine.**

---

## Icons & Brand Assets / Ikon & Aset Brand

**English** All official Rakta.js icons and brand assets are in `docs/assets/`. Here is how to use them in various contexts.

**Bahasa Indonesia** Semua ikon dan aset brand resmi Rakta.js ada di `docs/assets/`. Begini cara menggunakannya di berbagai konteks.

### Available Assets / Aset yang Tersedia

| File | Size | Purpose |
| --- | --- | --- |
| `docs/assets/Rakta.js.svg` | Scalable | Logo utama (udang Cirebon) dipakai di web, dokumen, presentasi |
| `docs/assets/Rakta.js.png` | 1080×1079 | Logo PNG resolusi tinggi print, social media |
| `docs/assets/raktajs_benner.png` | Full width | Banner untuk GitHub README, header website |
| `docs/assets/favicon.ico` | 48×48 | Browser tab icon |
| `docs/assets/favicon-16x16.png` | 16×16 | Favicon kecil |
| `docs/assets/favicon-32x32.png` | 32×32 | Favicon medium |
| `docs/assets/android-chrome-192x192.png` | 192×192 | Android home screen icon / PWA icon |
| `docs/assets/android-chrome-512x512.png` | 512×512 | Android splash screen / large PWA icon |
| `docs/assets/apple-touch-icon.png` | 180×180 | iOS/iPadOS home screen icon |

### Cara Pakai di HTML / Using in HTML

```html
<!-- Favicon lengkap untuk semua platform -->
<link rel="icon" type="image/x-icon" href="/favicon.ico" sizes="48x48" />
<link rel="icon" type="image/png" href="/favicon-16x16.png" sizes="16x16" />
<link rel="icon" type="image/png" href="/favicon-32x32.png" sizes="32x32" />
<link rel="icon" type="image/svg+xml" href="/rakta-logo.svg" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180" />
<link rel="manifest" href="/site.webmanifest" />
<meta name="theme-color" content="#e11d48" />
<meta name="msapplication-TileColor" content="#e11d48" />
```

**English** When you create a new app with `create-rakta-app`, all of these icon files are automatically placed in your `public/` folder. The `layout.tsx` is pre-configured to reference them. You do not need to do anything manually.

**Bahasa Indonesia** Saat kamu membuat app baru dengan `create-rakta-app`, semua file icon ini otomatis ditempatkan di folder `public/`. `layout.tsx` sudah dikonfigurasi untuk mereferensikannya. Kamu tidak perlu melakukan apapun secara manual.

### Pakai di Vercel / Netlify

**English** Vercel and Netlify automatically serve files from the `public/` folder at the root URL. Icons will be available at `yourdomain.com/favicon.ico`, `yourdomain.com/rakta-logo.svg`, etc. The `site.webmanifest` tells browsers how to install your app as a PWA and which icon to show on the home screen.

**Bahasa Indonesia** Vercel dan Netlify otomatis menyajikan file dari folder `public/` di URL root. Icon akan tersedia di `yourdomain.com/favicon.ico`, `yourdomain.com/rakta-logo.svg`, dll. `site.webmanifest` memberi tahu browser cara menginstal app sebagai PWA dan icon mana yang ditampilkan di home screen.

```bash
# Setelah deploy, icon tersedia di:
https://yourapp.vercel.app/favicon.ico
https://yourapp.vercel.app/favicon-32x32.png
https://yourapp.vercel.app/android-chrome-192x192.png
https://yourapp.vercel.app/android-chrome-512x512.png
https://yourapp.vercel.app/apple-touch-icon.png
https://yourapp.vercel.app/rakta-logo.svg
https://yourapp.vercel.app/site.webmanifest
```

---

## Author / Penulis

**English** ,  Created by **Rhein Sullivan** (Muhammad Rizky Ramadhan). Built with a Cirebon soul, a red identity, and a frontend-first mindset. Lead of **Vyagra Nexus™**, a software development team based in Cirebon and South Jakarta, Indonesia.

**Bahasa Indonesia** ,  Dibuat oleh **Rhein Sullivan** (Muhammad Rizky Ramadhan). Dibangun dengan jiwa Cirebon, identitas merah, dan pikiran yang mengutamakan frontend. Ketua **Vyagra Nexus™**, tim pengembangan perangkat lunak berbasis di Cirebon dan Jakarta Selatan, Indonesia.

---

## License / Lisensi

MIT — see [LICENSE](./LICENSE)
